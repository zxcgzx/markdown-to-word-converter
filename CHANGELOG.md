# Changelog

## 5.1.1 — 双层工具栏与点击可达性修复

### 工具栏重新规划

- 将原来拥挤的单行常驻工具栏拆分为上下两层。
- 第一层只保留文件、文档名、视图和输出，保证“下载 Word”始终处于清晰、稳定的位置。
- 第二层集中标题、粗体、斜体、列表、公式、表格和 AI 修复等编辑能力。
- 将“清空”移动到编辑行最右侧，并使用弱危险色与常用编辑动作拉开距离。

### 点击区域与遮挡修复

- 移除工具栏横向滚动依赖，改为 `overflow: visible`，不再裁切按钮。
- 输出组取消右侧 `sticky` 覆盖，不再压住视图切换或相邻按钮。
- 格式按钮统一为至少 `34 × 36 px`，普通工具按钮至少 36 px 高。
- 补充清晰的键盘焦点环，确保鼠标、触控板和键盘均可稳定操作。
- “更多”菜单关闭时完全退出布局；打开后限制最大高度、支持滚动并保持在视口内。

### 响应式策略

- 1180 像素及以下将低频编辑能力收纳到“更多”，避免静默隐藏。
- 900 像素及以下隐藏“新建”，仍保留“打开”、文档名、视图和 Word 输出。
- 680 像素及以下只保留打开、三种视图和 Word 下载，编辑工具行完全移除。
- 桌面和移动端原有视图记忆键保持不变，覆盖更新不会仅因本补丁清空现有偏好。

### 测试

- 新增 `tests/toolbar-layout.test.js`，自动测试总数扩展为 53 项。
- 浏览器回归在 1920、1366、1180、1024、800 和 390 像素宽度执行 110 项检查。
- 检查覆盖按钮重叠、中心点可点击、页面横向溢出、更多菜单视口边界、常驻吸顶、视图切换和格式操作。
- 浏览器回归结果为 110 / 110 通过，控制台和页面错误均为 0。

## 5.1.0 — 导出闭环与精确定位

### 导出前检查

- 新增独立 `js/preflight.js` 检查引擎。
- Word 主按钮增加“检查通过 / 有提醒 / 有错误”三种就绪状态和数量徽标。
- 首次导出存在问题时打开工作区内联检查面板，不再直接生成可能有误的 DOCX。
- 支持检查公式渲染错误、未闭合公式、未闭合代码围栏、复杂公式线性化、标题层级跳跃、超宽表格、空链接和图片兼容性。
- 每条带源码位置的问题支持一键定位；用户确认风险后仍可选择继续导出。
- 核心解析依赖缺失时生成不可误定位的阻断项，避免被错误指向文档第一个字符。

### 公式源码定位

- 为每个公式记录原始 Markdown 的起止位置和公式序号。
- 公式诊断中的“定位源码”可自动切换视图、选中源码、滚动到对应位置并报告行列。
- 预览中的错误公式支持鼠标点击和键盘聚焦后直接定位。
- 独立 `[ ... ]` 公式修复改为保留原始空白和缩进，只插入缺少的两个反斜杠。
- 增加标准化偏移到原始源码偏移的映射，多处松散公式可分别准确定位。
- 修复已修复公式渲染失败时错误选中范围过大的问题。

### 文档名与保存状态

- 顶部新增可编辑文档名，并统一用于 `.md` 和 `.docx` 文件名。
- 打开文件、恢复草稿、新建和导出时同步维护文档名。
- 将“浏览器草稿已保存”和“Markdown 文件已下载”拆分为两种明确状态。

### 响应式与设置

- 681～1180 像素宽度下增加“更多”菜单，低频工具不再静默隐藏。
- 桌面与移动端分别记忆编辑、分栏和预览模式；手机首次默认编辑模式。
- 保留唯一设置对话框，并增加界面、Word、AI、快捷键、账户分类导航。
- 分类标签支持左右方向键切换，视图按钮同步更新 `aria-pressed`。
- Word 页边距输入精度调整为 `0.01`，允许准确保存 `2.54 cm` 等常见值。
- 补齐 `.visually-hidden`，修复移动端隐藏标签占位和工具栏重叠。

### 稳定性

- 所有 `localStorage` 读写改为带异常保护的安全包装。
- 代码围栏关闭规则更严格：带尾随正文的反引号行不会错误结束围栏。
- 行内代码掩码保持 JavaScript UTF-16 偏移，含 emoji 时定位仍准确。
- 通用 OpenAI 兼容请求不再强制发送 `temperature`，提升接口兼容性。
- 更新默认 AI 预设，同时仍允许用户完全覆盖接口和模型。

### 测试

- 自动测试扩展为 47 项：16 项融合页面测试、16 项公式引擎测试、15 项导出前检查测试。
- 浏览器回归覆盖 46 个桌面、公式、导出、设置、移动端和会话场景。
- 四个本地 JavaScript 文件通过语法检查。
- HTML 与 CSS 通过结构解析、ID 唯一性和声明语法检查。

## 5.0.0 — 融合体验版

- 增加常驻顶部操作栏，按文件、编辑、视图和输出分组。
- 将“下载 Word”设为页面唯一主按钮。
- 默认空白启动，示例与草稿恢复只在空预览出现。
- 用大纲下拉框替换悬浮缩略导航。
- 增加可记忆分栏、同步滚动、公式状态详情和移动端精简工具栏。
- AI 与表格改为工作区内联工具，低频配置集中到一个设置窗口。
- DOCX 导出单独解析公式，输出可编辑上下标文本。

## 4.0.0 — 融合增强版

- 恢复原版密码入口、用户身份、玻璃态视觉和四套主题。
- 引入公式预保护引擎，支持四种常用公式边界和独立 `[ ... ]` 修复。
- 重构 DOCX 生成链路并增加 AI、表格、草稿、拖放、快捷键和大纲能力。
