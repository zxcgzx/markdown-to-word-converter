# 更新记录

## v5.4 · Page Preview, Images and Publishing

### A4 / Letter 页面预览

- 新增网页预览与页面预览双模式。
- 支持 A4、Letter、纵向、横向和四个独立页边距。
- 页面预览同步 Word 字体、字号和行距。
- 显示预计页数和页面宽度溢出提醒。
- 大纲跳转同时兼容网页和 A4 页面模式。
- 明确说明页面预览属于块级分页估算，最终分页由 Word/WPS 决定。

### 显式分页

- 新增工具栏、更多菜单和 `Alt + P` 分页入口。
- 分页标记同时作用于网页提示、A4 页面和 DOCX `PageBreak`。
- 修复文档末尾分页符和连续分页符丢失空白页的问题。
- 修复分页动作被多个委托监听器重复执行的问题。

### 图片素材与 Word 嵌入

- 新增每文档独立图片素材库。
- 支持文件选择、剪贴板粘贴和拖放图片。
- 支持通过网络 URL 下载并保存本地素材，也可保留普通外部地址。
- 支持小、中、适应页面和原始尺寸。
- 支持素材使用状态、重新插入、删除和清理未使用素材。
- WebP、SVG 和超大图片自动转换或缩放到 Word 兼容格式。
- 文档副本会复制所引用的素材并重写引用 ID。
- 删除文档时级联删除其版本和素材。
- 工作区备份 schema 升级到 v2，并包含图片素材。
- Word 导出前解析本地素材，远程图片按设置尝试下载。
- 图片宽度被限制在当前纸张可打印宽度以内。
- 修复清空文件选择器时 Live `FileList` 同步变空、导致图片未导入的问题。

### 文档模板

- 新增通用报告、实验报告、论文初稿、会议纪要、标准操作规程和横向数据表六种模板。
- 模板同步推荐纸张、方向、字体、字号、行距和页边距。
- 替换已有内容前创建版本保护点并要求确认。

### 长文性能

- 新增自动、实时、长文优化和手动刷新四种预览策略。
- 自动模式在长文中降低 Markdown、公式、A4 分页和检查刷新频率。
- 手动模式显示待刷新状态，并提供明确的刷新按钮。

### v5.3 数据迁移

- 新增对 v5.3 IndexedDB `md2word-workspace-v5.3` 的只读迁移。
- 新增对真实 v5.3 localStorage 键 `md2word.workflow.documents.v5.3` 的迁移。
- 迁移嵌入式历史版本、光标、视图和最后打开文档。
- 旧数据保留不删；已有 v5.4 同 ID 文档不会被覆盖。
- 保留对早期开发键 `md2word.workspace.fallback.v5.3` 的兼容读取。

### 导出检查与稳定性

- 导出检查新增本地素材缺失、远程图片失败、相对路径、`blob:` 图片和页面溢出诊断。
- 原始 HTML `<img>` 同样进入检查链路。
- 导出前等待图片素材解析和 A4 预览更新。
- Word 页面属性使用对应纸张尺寸、方向和 twip 页边距。

### 测试

- JavaScript 语法检查：7 / 7 通过。
- Node 自动测试：118 / 118 通过。
- Chromium 主集成回归：79 / 79 通过。
- Chromium 主题与响应式布局矩阵：91 / 91 通过。
- HTML、CSS、资源、可访问名称和最终 ZIP 解压副本均完成复测。

## v5.3 · Reliable Workflow

- 新增多文档中心、版本历史、智能粘贴、工作区备份与诊断。
- 新增 IndexedDB、localStorage 和临时内存三级存储。
- 新增品牌区自动收起、统一 SVG 图标和 Word 导出成功回执。

## v5.2.3 · Bare Inline TeX Recovery

- 修复 `(C_\eta=1%C_{\text{curtail}})` 等高置信度裸行内 TeX。
- 自动规范公式上下文中的数值百分号。
- 增加精确源码定位、标准语法写回和 Word 可编辑下标。

## v5.2.2 · Global Typography System

- 建立八级全局字体体系、独立文档排版和三种界面密度。
- 统一中文、英文、数字、快捷键和响应式排版。

## v5.2 · Brand Login and Advanced Interaction

- 重构高级双栏登录入口。
- 新增命令面板、专注模式、本机自动进入和分享码内联导入。

## v5.1 · Export Preflight and Toolbar

- 新增导出前检查、公式源码定位和 Word 就绪状态。
- 将工具栏重构为连续式双层 Command Deck。

## v5.0 · Original UI Fusion

- 融合原版密码、主题和玻璃态界面与新版公式、Word 导出内核。
