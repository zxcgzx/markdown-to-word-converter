# Changelog

## 5.0.0 — 融合体验版

### 聚焦式工作流

- 增加真正的常驻顶部操作栏，按“文件、编辑、视图、输出”重新分组。
- 将“下载 Word”设为页面唯一主按钮，避免写完后滚动寻找导出入口。
- 默认空白启动；公式示例与草稿恢复只出现在空预览区。
- 移除悬浮缩略导航，改为预览标题栏中的大纲下拉框。
- 移除底部重复功能组，常用操作直接位于顶部，低频选项归入统一设置。

### 编辑与预览

- 增加可拖动分隔条，并分别记忆桌面端和移动端分栏比例。
- 双击分隔条可恢复均分，键盘方向键也可微调。
- 增加可关闭的编辑器与预览按比例同步滚动。
- 编辑、分栏、预览三种模式保留并强化。
- 普通反馈统一进入底部紧凑状态栏；只有错误生成可见错误卡片。
- 移除配额、权限额度和非任务型状态噪音。

### 公式诊断

- 预览标题栏直接显示“公式数量 · 错误数量 · 边界修复数量”。
- 点击状态可展开公式来源、KaTeX 错误、自动修复说明和标准边界写回。
- 依赖加载异常、渲染失败和边界退化的反馈更加明确。
- 保留公式预保护引擎，支持 `$...$`、`$$...$$`、`\(...\)` 与 `\[...\]`。
- 继续排除围栏代码块与行内代码中的伪公式。

### 工具与设置

- AI 修复与表格转换改为工作区内联工具，不再各自打开模态框。
- AI 修复保存配置后可直接处理选区或全文。
- 主题、草稿、公式修复、同步滚动、Word 导出、AI、快捷键和账户集中到唯一的统一设置窗口。
- 保留原版密码入口、分享码、用户身份、玻璃态视觉和四套主题。

### 移动端

- 680 像素以下的顶部常驻栏只保留“打开、视图切换、下载 Word”。
- 低频设置和退出入口移动到底部“设置与账户”。
- 修正工具栏和页面横向溢出。

### Word 导出

- 导出阶段单独解析公式，不读取 KaTeX 重复文本。
- 使用 Word `TextRun` 的 `subScript` 与 `superScript` 输出可编辑上下标。
- 不再插入“请手动添加公式”的占位提示。
- 化学式、变量和常见符号可编辑；复杂分式与矩阵保留为线性文本。

### 测试

- 自动测试扩展至 21 项：12 项融合页面与交互静态测试，9 项公式引擎测试。
- 三个 JavaScript 核心文件通过语法检查。
- 增加桌面端、移动端、粘性工具栏、唯一主按钮和运行错误的浏览器级烟雾检查。

## 4.0.0 — 融合增强版

### 界面与操作

- 恢复原版风格的密码验证入口、分享码粘贴和用户状态展示。
- 恢复玻璃态渐变标题区、双栏编辑预览和底部分组控制面板。
- 恢复暖阳琥珀、经典浅林、现代黑金、极光幻彩四套主题。
- 增加顶部快速格式工具、编辑/分栏/预览模式、大纲导航和同步滚动。
- 增加可拖动分隔条、拖放文件、自动保存、快捷键和运行状态栏。

### 公式

- 新增公式预保护引擎，避免 Markdown 解析破坏 `\[...\]` 与 `\(...\)`。
- 支持 `$...$`、`$$...$$`、`\(...\)`、`\[...\]`。
- 排除围栏代码块和行内代码中的公式标记。
- 支持修复独立 `[ ... ]` TeX 块并写回标准边界。
- 增加公式数量、错误和修复数量诊断。
- KaTeX 渲染失败时保留可见源码和错误信息。

### Word 导出

- 重构 DOCX 生成链路，支持标题、段落、列表、引用、表格、代码和链接。
- 公式转换为可编辑文本及上下标运行，不再插入手动公式占位提示。
- 增加正文字体、字号、行距和页面边距设置。

### AI 与工具

- 保留 AI 修复入口，支持多个预设与自定义 OpenAI 兼容接口。
- 优先修复当前选区，无选区时处理全文。
- 增加 AI 结果对比、编辑、复制与应用。
- 增加 TSV、CSV、Markdown 表格转换与插入。
