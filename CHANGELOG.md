# Changelog

## 5.2.0 — 品牌入口与高级交互

### 全新登录体验

- 将原来的功能说明式密码页重构为“品牌介绍 + 登录操作”双栏结构，登录按钮成为页面唯一高亮主动作。
- 品牌区只保留一句产品定位、三项核心能力和一条版本更新，移除重复徽章、提示卡和多层功能卡。
- 使用统一线性 SVG 图标、克制动效、轻量光晕与主题化背景，减少 Emoji 和组件拼装感。
- 手机端压缩为品牌标题、能力标签和登录卡片，320～430 像素宽度均无横向溢出。

### 登录前高级交互

- 增加登录前主题切换，暖阳琥珀、经典浅林、现代黑金和极光幻彩与工作区保持同步。
- 增加“在这台设备上自动进入”，并在统一设置的账户分类中显示和清除状态。
- 自动清理无效或已从访问配置中移除的记忆状态；浏览器禁止持久存储时不阻断当前会话。
- 分享码改为登录卡片内联展开，支持直接读取剪贴板或手动粘贴。
- 分享码日期使用严格日历校验，可识别格式错误、无效日期和过期状态。
- 增加 Caps Lock 提示、精细密码错误、输入框 `aria-invalid`、验证加载态、成功态和平滑登录转场。
- 登录页和命令面板增加键盘焦点循环，背景元素不会在弹层打开时意外获得焦点。

### 全局命令面板

- 新增 `Ctrl / Command + K` 命令面板，可搜索并执行文件、导出、视图、公式、表格、AI、主题、设置和专注模式等操作。
- 命令搜索采用权重排序，标签精确匹配和标签包含优先于描述中的偶然关键词。
- 支持方向键、Home、End、Enter、Escape 和 Tab 键盘操作，并恢复打开前焦点。
- 面板在手机视口内自动收缩，不产生横向溢出。

### 专注模式

- 新增 `Ctrl / Command + Shift + F` 专注模式。
- 隐藏品牌标题、编辑工具行、文档名和非必要状态，仅保留当前视图、工作区、Word 主按钮与退出入口。
- 支持快捷键、退出按钮和 Escape 退出；打开设置或内联工具时自动恢复普通模式。
- 预览视图进入专注时将焦点交给预览区域，编辑和分栏视图交给编辑器。

### 稳定性与兼容性

- v5.2 新增独立 `css/experience.css`，在基础样式和 Command Deck 样式之后加载。
- 所有本地资源统一使用 `?v=5.2` 查询参数，减少 GitHub Pages 和浏览器旧缓存造成的界面回退。
- 保留 v5.1 的会话、草稿、设置、桌面视图和移动视图存储键；本机自动进入使用新的 v5.2 键。
- 修复命令搜索“预览”查询可能优先命中低相关命令的问题。
- 修复分享码日期只检查字符串格式、未验证真实日期的问题。
- 修复专注模式在预览视图仍把焦点交给隐藏编辑器的问题。
- 修复持久存储不可用时自动进入请求可能影响正常登录的问题。

### 测试

- Node 自动测试扩展为 60 项，全部通过。
- 浏览器回归执行 299 项检查，覆盖桌面登录、主题、分享码、自动进入、命令面板、专注模式、公式、导出预检、设置、退出和移动端高级交互。
- 登录布局矩阵覆盖 10 个视口，工作区矩阵覆盖 12 个视口，范围为 320～1920 像素。
- 四套主题分别在 1440 和 390 像素检查，无横向溢出、主操作丢失或运行错误。
- HTML、CSS、本地资源和四个 JavaScript 文件均通过最终静态检查。

## 5.1.2 — Command Deck 工具栏视觉与结构优化

### 连续式顶部操作面

- 将 v5.1.1 的多块卡片式排列重构为一块连续的 Command Deck，降低边框、留白和分组标签造成的视觉噪声。
- 文档操作与编辑操作仍保持上下分层，但两行均强制占满可用宽度，避免旧样式缓存导致左右并排和左侧纵向堆叠。
- 文档行采用 `file | name | view | output` 命名网格，文档名成为可伸缩中心区域。
- 编辑行改为连续工具带，常用格式、表格、AI 修复与清空形成清晰的从左到右操作路径。

### 视觉层级与点击体验

- 扁平化文件、视图和输出组的嵌套卡片，仅用细分隔线表达功能边界。
- 文档名增加文档图标、扩展名胶囊、悬停态和聚焦态，使其更像当前文档入口而不是普通按钮。
- 视图切换统一为紧凑分段控件；“下载 Word”继续作为唯一高亮主按钮。
- “清空”保持在编辑行最右侧并使用弱危险样式，减少与高频编辑操作混淆。
- 保留足够的按钮高度、焦点环和指针命中区域，未通过缩小控件来换取空间。

### 响应式布局

- 1321 像素以上显示完整 Command Deck。
- 1181～1320 像素将保存 Markdown 与复制富文本收入“更多”。
- 901～1180 像素保留打开、文档名、视图、Word 与 H/B/I/更多。
- 681～900 像素使用平衡的 `2 × 2` 文档网格：打开/Word、文档名/视图，不再形成左侧长纵列。
- 680 像素以下严格保留打开、编辑、分栏、预览与 Word 五个控件，并隐藏编辑工具行。
- “更多”菜单补齐保存 Markdown、复制富文本、公式、表格、AI、清空和设置等隐藏操作。

### 缓存与稳定性

- 新增独立 `css/toolbar.css`，在 `app.css` 之后加载，专门承载工具栏结构覆盖。
- `app.css`、`toolbar.css` 与本地脚本增加 `?v=5.1.2` 查询参数，降低 GitHub Pages 或浏览器旧缓存造成布局回退的概率。
- 关键全宽行、网格和输出定位规则使用明确的高优先级覆盖，兼容从 v5.1.1 整体覆盖升级。

### 测试

- Node 自动测试扩展为 55 项，全部通过。
- 工具栏布局矩阵覆盖 24 个视口宽度（320～1920 像素），无横向溢出、可见控件重叠或中心点点击失效。
- 另在 9 个代表宽度执行视图、格式、更多菜单和 DOCX 保存回归。
- 完整浏览器功能烟雾测试 31 / 31 通过，页面错误与控制台错误均为 0。
- HTML、CSS、本地资源、JavaScript 语法和发布包解压副本均通过最终复检。

## 5.1.1 — 双层工具栏与点击可达性修复

### 工具栏重新规划

- 将原来拥挤的单行常驻工具栏拆分为上下两层。
- 第一层只保留文件、文档名、视图和输出，保证“下载 Word”始终处于清晰、稳定的位置。
- 第二层集中标题、粗体、斜体、列表、公式、表格和 AI 修复等编辑能力。
- 将“清空”移动到编辑行最右侧，并使用弱危险色与常用编辑动作拉开距离。

### 点击区域与遮挡修复

- 移除工具栏横向滚动依赖，改为 `overflow: visible`，不再裁切按钮。
- 输出组取消右侧 `sticky` 覆盖，不再压住视图切换或相邻按钮。
- 格式按钮统一为至少 `34 × 36 px`，普通工具按钮至少 36 px 高。
- 补充清晰的键盘焦点环，确保鼠标、触控板和键盘均可稳定操作。
- “更多”菜单关闭时完全退出布局；打开后限制最大高度、支持滚动并保持在视口内。

### 响应式策略

- 1180 像素及以下将低频编辑能力收纳到“更多”，避免静默隐藏。
- 900 像素及以下隐藏“新建”，仍保留“打开”、文档名、视图和 Word 输出。
- 680 像素及以下只保留打开、三种视图和 Word 下载，编辑工具行完全移除。
- 桌面和移动端原有视图记忆键保持不变，覆盖更新不会仅因本补丁清空现有偏好。

### 测试

- 新增 `tests/toolbar-layout.test.js`，自动测试总数扩展为 53 项。
- 浏览器回归在 1920、1366、1180、1024、800 和 390 像素宽度执行 110 项检查。
- 检查覆盖按钮重叠、中心点可点击、页面横向溢出、更多菜单视口边界、常驻吸顶、视图切换和格式操作。
- 浏览器回归结果为 110 / 110 通过，控制台和页面错误均为 0。

## 5.1.0 — 导出闭环与精确定位

### 导出前检查

- 新增独立 `js/preflight.js` 检查引擎。
- Word 主按钮增加“检查通过 / 有提醒 / 有错误”三种就绪状态和数量徽标。
- 首次导出存在问题时打开工作区内联检查面板，不再直接生成可能有误的 DOCX。
- 支持检查公式渲染错误、未闭合公式、未闭合代码围栏、复杂公式线性化、标题层级跳跃、超宽表格、空链接和图片兼容性。
- 每条带源码位置的问题支持一键定位；用户确认风险后仍可选择继续导出。
- 核心解析依赖缺失时生成不可误定位的阻断项，避免被错误指向文档第一个字符。

### 公式源码定位

- 为每个公式记录原始 Markdown 的起止位置和公式序号。
- 公式诊断中的“定位源码”可自动切换视图、选中源码、滚动到对应位置并报告行列。
- 预览中的错误公式支持鼠标点击和键盘聚焦后直接定位。
- 独立 `[ ... ]` 公式修复改为保留原始空白和缩进，只插入缺少的两个反斜杠。
- 增加标准化偏移到原始源码偏移的映射，多处松散公式可分别准确定位。
- 修复已修复公式渲染失败时错误选中范围过大的问题。

### 文档名与保存状态

- 顶部新增可编辑文档名，并统一用于 `.md` 和 `.docx` 文件名。
- 打开文件、恢复草稿、新建和导出时同步维护文档名。
- 将“浏览器草稿已保存”和“Markdown 文件已下载”拆分为两种明确状态。

### 响应式与设置

- 681～1180 像素宽度下增加“更多”菜单，低频工具不再静默隐藏。
- 桌面与移动端分别记忆编辑、分栏和预览模式；手机首次默认编辑模式。
- 保留唯一设置对话框，并增加界面、Word、AI、快捷键、账户分类导航。
- 分类标签支持左右方向键切换，视图按钮同步更新 `aria-pressed`。
- Word 页边距输入精度调整为 `0.01`，允许准确保存 `2.54 cm` 等常见值。
- 补齐 `.visually-hidden`，修复移动端隐藏标签占位和工具栏重叠。

### 稳定性

- 所有 `localStorage` 读写改为带异常保护的安全包装。
- 代码围栏关闭规则更严格：带尾随正文的反引号行不会错误结束围栏。
- 行内代码掩码保持 JavaScript UTF-16 偏移，含 emoji 时定位仍准确。
- 通用 OpenAI 兼容请求不再强制发送 `temperature`，提升接口兼容性。
- 更新默认 AI 预设，同时仍允许用户完全覆盖接口和模型。

### 测试

- 自动测试扩展为 47 项：16 项融合页面测试、16 项公式引擎测试、15 项导出前检查测试。
- 浏览器回归覆盖 46 个桌面、公式、导出、设置、移动端和会话场景。
- 四个本地 JavaScript 文件通过语法检查。
- HTML 与 CSS 通过结构解析、ID 唯一性和声明语法检查。

## 5.0.0 — 融合体验版

- 增加常驻顶部操作栏，按文件、编辑、视图和输出分组。
- 将“下载 Word”设为页面唯一主按钮。
- 默认空白启动，示例与草稿恢复只在空预览出现。
- 用大纲下拉框替换悬浮缩略导航。
- 增加可记忆分栏、同步滚动、公式状态详情和移动端精简工具栏。
- AI 与表格改为工作区内联工具，低频配置集中到一个设置窗口。
- DOCX 导出单独解析公式，输出可编辑上下标文本。

## 4.0.0 — 融合增强版

- 恢复原版密码入口、用户身份、玻璃态视觉和四套主题。
- 引入公式预保护引擎，支持四种常用公式边界和独立 `[ ... ]` 修复。
- 重构 DOCX 生成链路并增加 AI、表格、草稿、拖放、快捷键和大纲能力。
